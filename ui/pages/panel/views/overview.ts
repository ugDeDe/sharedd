/* Экран «Обзор»: метрики, топология пула, статистика, история банов.
   TODO(panel-core): реализация. Контракт: render(o) вызывается роутером
   на каждом обновлении (раз в 5 с) с объектом /panel/api/overview. */
export function render(_o: any): void {}
